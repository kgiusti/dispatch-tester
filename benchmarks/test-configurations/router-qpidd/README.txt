A router fronting access to the qpidd broker.

Run qpidd with '--config qpidd.conf'

Clients connect to the router using port 5672.
The broker listens on port 10000 for router connections.

The default message address prefix is "benchmark".
